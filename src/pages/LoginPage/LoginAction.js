

const initialState = {
    optionRegister: false,

};

//Sin terminar
const changeOptionRegister = (state = initialState, action) => {
    switch (key) {
        case OPTION_REGISTER:
            
            break;
    
        default:
            return state
    }
}